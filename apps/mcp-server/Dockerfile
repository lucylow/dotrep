FROM node:20-slim

WORKDIR /app

# The MCP server uses the existing dotrep-v2 structure
# This Dockerfile should be built from the repo root
# For docker-compose, we'll use the existing dotrep-v2 Dockerfile
# This is a placeholder - in practice, use the dotrep-v2 Dockerfile

# Alternative: Build from dotrep-v2 directory
# docker-compose should use: context: ../dotrep-v2

RUN echo "MCP Server - use dotrep-v2 Dockerfile or build separately"

EXPOSE 3001

